# Remediation-Plan — visual-fx-web-components

Quelle: ./audit.html vom 2026-09-22 · Branch: main · erstellt: 2026-09-22
Baseline: `pnpm lint` ✗ (24 Fehler, dazu Warnungen und Infos, alle in `audit.html`, siehe unten) · `pnpm typecheck` ✓ · `pnpm build` ✓ · `pnpm test` ✓ · `pnpm e2e` ✓
Verify: `pnpm lint` · `pnpm typecheck` · `pnpm build` · `pnpm test` · `pnpm e2e` (zusammen: `pnpm verify`) · ab Paket 2 auf dieser Maschine als `E2E_SKIP_WEBKIT=1 pnpm verify` (WebKit nur in CI, siehe »Entscheidungen«)
Arbeitsverzeichnis: /tmp/claude-1000/-home-spw-spaceland-visual-fx-web-components/09543aa0-4d6e-4d0f-8ffc-49168ab25958/scratchpad (Diffs und Verify-Logs, außerhalb der Versionierung)
Paketdetails: docs/remediation/paket-<N>.md — je Paket eine Datei, angelegt von dessen Zug 0
Scope: 19 von 20 Findings (2 high, 10 medium, 7 low), einschließlich der als »Optimierungspotenzial« geführten PERF-001, PERF-002 und CONS-001 · ausgenommen: PERF-003 (bewusste Entscheidung, siehe unten)
Scope-Regel: alles, jede Severity und jede Kategorie, außer der bewussten Entscheidung für eventize und signalize — gilt auch für Befunde, die erst im Lauf auffallen
Kaltstarts: 5 Pakete × mindestens 3 Agenten ≈ 15, je Nachrunde zwei mehr · 4,75 Findings je Paket der ersten vier, Paket 5 trägt nur Folgen
Stand (2026-09-22): Lauf abgeschlossen — 6 Pakete committet (cdafb38..999823e), nichts blockiert, Befund-Queue leer, Report in docs/remediation/20260922-remediation-report.md

Diese Datei führt einen Lauf des Skills `js-ts-audit-remediation` und hält
seinen Stand. Wer hier weiterarbeitet: diesen Skill laden, die eingetragenen
Hashes gegen `git log --oneline` halten, beim obersten Paket ohne `[x]`
einsteigen. Der Lauf ist erst fertig, wenn auch »Offene Befunde« leer ist.
Statusmarken: `[ ]` offen · `[~]` Detailplan steht, Umsetzung läuft · `[x]`
erledigt · `[r]` committet, Review wird nachgezogen · `[!]` blockiert.

## Entscheidungen
- PERF-003 wird nicht umgesetzt: Die Nutzung von `@spearwolf/eventize` und `@spearwolf/signalize` in `OffscreenWorkerDisplay` ist eine bewusste Entscheidung *für* diese Bibliotheken; das Gewicht des Worker-Bundles wird dafür in Kauf genommen. Im Audit als acknowledged zu führen (2026-09-22)
- RES-001: Beim Disconnect wird das Beenden des Workers mit einer Gnadenfrist von einem rAF eingeplant; ein Re-Connect innerhalb der Frist behält den Worker, ein späterer Re-Connect bekommt einen frischen `<canvas>` und einen frischen Worker. Öffentliche Methode `dispose()` (2026-09-22)
- DEP-001: Die Quell-Subpfade `./RainbowLineElement.js` und `./RainbowLineWorkerDisplay.js` bleiben öffentliche API; `@spearwolf/eventize` und `@spearwolf/offscreen-display` werden optionale `peerDependencies` von `rainbow-line`, `astro` wird `peerDependency` von `astro-rainbow-line` (2026-09-22)
- BUILD-001: Minor-Bumps im Lauf — `offscreen-display` 0.3.0, `rainbow-line` 0.5.0, `astro-rainbow-line` passend nachgezogen samt vendored `rainbow-line-v0.5.0.js`, Default-Pfad und README; Changelogs datiert. Veröffentlicht wird erst durch den Push des Nutzers (2026-09-22)
- TEST-001: Die e2e-Suite bekommt zusätzliche Playwright-Projekte für WebKit und Firefox; `pnpm playwright:install` und CI installieren beide mit. Die Vitest-Paket-Tests bleiben bei Chromium. Laufen WebKit oder Firefox auf dieser Maschine nicht, ist das eine Rückfrage, kein stilles Weglassen (2026-09-22)
- CORR-005: Der Worker bekommt die physische Pixelgröße und zusätzlich `pixelRatio`; `rainbow-line` rechnet seine CSS-Pixel-Werte (`color-slice-width`) damit um. Das ändert die Semantik von `onResize` für Subklassen und gehört in den Minor-Bump (2026-09-22)
- READ-001: Die Kehrwert-Semantik von `cycle-colors-repeat < 1` bleibt erhalten, wird als benannte Funktion geschrieben und im README dokumentiert (2026-09-22)
- BUILD-003: `TODO.md` wird aus dem Repo genommen und ignoriert; `pnpm make:todo` bleibt als lokales Werkzeug (2026-09-22)
- Baseline: `audit.html` wird aus dem Biome-Check ausgenommen — ein generierter Report, kein Quelltext. Das macht `pnpm lint` grün, bevor das erste Finding angefasst wird (2026-09-22)
- TEST-001, WebKit: Playwright-WebKit startet auf dieser Maschine nicht (CachyOS hat ICU 78 und kein flite, WebKit verlangt `libicu*.so.74` und `libflite.so.1`). Das `webkit`-Projekt steht trotzdem in der e2e-Config und läuft in CI; lokal lässt `E2E_SKIP_WEBKIT=1` es aus, die Config meldet das Auslassen mit einer Zeile, und CI ignoriert die Variable. Verify dieses Laufs ist deshalb `E2E_SKIP_WEBKIT=1 pnpm verify`; den WebKit-Pfad prüft erst CI nach dem Push des Nutzers (2026-09-22)

## Konventionen
Gelten für jede Zeile, die in diesem Lauf entsteht — Code, Kommentare,
Dokumentation, CHANGELOG, Migrations-Hinweise, Commit-Messages:
- Inline-Kommentare sind erwünscht, wo sie erklären, *warum* etwas so ist.
- Keine Finding-IDs, auch nicht in der Commit-Message. Sie gehören diesem einen
  Audit, sind danach tot, und die Commit-Message überdauert den Lauf. Sie leben
  in diesem Plan und sonst nirgends; die Verbindung zwischen Finding und Commit
  trägt das Feld `Hash:` unter dem Paket — in genau der Richtung, in der jemand
  sie später sucht. Eine Commit-Message sagt in eigenen Worten, was sie ändert.
- Kein Rückblick auf den Vorzustand: kein »früher«, kein »statt bisher«, kein
  »im Zuge des Audits umgestellt«. Der Test: Ergibt der Satz für jemanden Sinn,
  der den Vorzustand nie gesehen hat? Dann bleibt er. Braucht er ihn, gehört er
  in die Commit-Message — die Historie ist bereits konserviert.
- Commit-Messages auf Englisch, klein geschrieben, im Stil von `git log`.
- Code-Stil nach `biome.json` (130 Spalten, single quotes, `.js`-Endungen an relativen Imports).
- Jede Änderung an einem Package bekommt ihren Eintrag unter »Unreleased« in dessen `CHANGELOG.md`; Paket 4 datiert die Abschnitte.

## Vorbestehende Fehler
- `pnpm lint` — 24 Fehler, 141 Warnungen, 101 Infos, ausschließlich in `audit.html` (generierter Report). Behoben in Paket 1 (cdafb38): `audit.html` ist aus Biome ausgenommen, alle Verify-Gates sind grün.

## Offene Befunde
Nebenbefunde aus den Paketen: was auch ohne diesen Lauf falsch war. Jeder
Eintrag wird beschlossen, bevor der Lauf endet — Paket oder Rückgabe ins Audit.
Ein leerer Abschnitt ist Abschlussbedingung, kein Zufall. Das Urteil am Ende
der Zeile misst den Eintrag an der Scope-Regel oben: `→ Scope`, `→ Audit`,
`→ Rückfrage`.
- [x] `packages/offscreen-display/src/lib/main/OffscreenDisplay.js:214` — Tippfehler im Kommentar `// TODO adpoptedCallback ?` (gemeint: `adoptedCallback`) · Paket 2 · low → Scope · in Paket 6 aufgenommen (Drain) · behoben in Paket 6 (999823e)
- [x] `packages/offscreen-display/src/lib/main/OffscreenDisplay.js:81` und `:167` — JSDoc »the unbiased context attributes« bzw. »the un-opinionated initial attributes« sagt nicht, was die Methode liefert · Paket 2 · low → Scope · in Paket 4 aufgenommen (gleiche Ursache wie TYPES-001) · behoben in Paket 4 (07f8a56)
- [x] `packages/rainbow-line/src/RainbowLineElement.js` (`createWorker()`, `new URL('rainbow-line.worker.js', import.meta.url)`) — der veröffentlichte Quell-Subpfad `./RainbowLineElement.js` lädt seinen Worker aus `.npm-pkg/src/rainbow-line.worker.js`, das nicht veröffentlicht wird (404, wenn ein Konsument `createWorker()` nicht überschreibt); vorbestehend seit `e57a13c`, die Import-Prüfung in `e2e/tests/npm-packages.spec.js` sieht `new URL(…)` und Side-Effect-Imports nicht · Paket 3 · medium → Scope · Hinweis aus Zug 0 von Paket 4: der Fix ändert auch `bundle.js` (es enthält den Basis-`createWorker()`), danach `packages/astro-rainbow-line/rainbow-line-v0.5.0.js` neu aus `bundle.js` kopieren; die Quell-Subpfade dokumentiert Paket 4 im rainbow-line-README bewusst nicht, das gehört zu diesem Fix · in Paket 6 aufgenommen (Drain) · behoben in Paket 6 (999823e)
- [x] `packages/rainbow-line/README.md:38` — Betreff im `mailto`-Link lautet `@spearwolf/offscreen-display` statt rainbow-line · Paket 3 · low → Scope · in Paket 4 aufgenommen (README-Prüfung aus DX-001) · behoben in Paket 4 (07f8a56)
- [x] `packages/rainbow-line/CHANGELOG.md` (Vergleichslinks am Ende, damals `:71-73`) — `unreleased` vergleicht ab `rainbow-line-v0.2.1` statt ab dem letzten Release, für `0.4.0` fehlt der Link · Paket 3 · low → Scope · in Paket 4 aufgenommen (steht in der Empfehlung von BUILD-001) · behoben in Paket 4 (07f8a56)
- [x] `packages/astro-rainbow-line/README.md:5` — der Pfad `${import.meta.env.BASE_URL}/js/…` ergibt einen doppelten Slash, weil Astros `BASE_URL` auf `/` endet · Paket 3 · low → Scope · in Paket 4 aufgenommen (README beschreibt einen Pfad, den `RainbowLine.astro:27` nicht baut — DX-001; dieselbe Zeile ändert Paket 4 für die Version) · behoben in Paket 4 (07f8a56)
- [x] `CLAUDE.md:68` — »esbuild outputs get a license banner from `scripts/makeBanner.mjs`« stimmt nur für rainbow-line; `packages/offscreen-display/scripts/build.mjs` setzt kein Banner · Paket 4 · low → Scope · in Paket 6 aufgenommen (Drain) · behoben in Paket 6 (999823e)
- [x] `CLAUDE.md:32` — »Test files are not type checked« gilt nur für `packages/*/test/`; `tsconfig.json` nimmt `e2e/tests/**/*.js` mit auf · Paket 4 · low → Scope · in Paket 6 aufgenommen (Drain) · behoben in Paket 6 (999823e)
- [x] `packages/offscreen-display/README.md:5` — »creates an _offline canvas_«, gemeint ist `OffscreenCanvas` · Paket 4 · low → Scope · in Paket 6 aufgenommen (Drain) · behoben in Paket 6 (999823e)
- [x] `packages/rainbow-line/src/RainbowLineWorkerDisplay.js:97` (`parseMessageData`) — `'color-slice-width' in data` wirft bei `null`/`undefined` einen TypeError, obwohl `display.parseMessageData(data)` davor `null` abfängt; die Elemente senden immer Objekte · Paket 4 · low → Scope · bei rainbow-line-Code vendored Bundle neu kopieren und Changelog in 0.5.0, solange nicht gepusht · in Paket 6 aufgenommen (Drain) · behoben in Paket 6 (999823e)
- [x] `e2e/server.mjs:36` und `:49` — `file.startsWith(dir)` prüft ohne Pfadtrenner (Nachbarverzeichnis mit gleichem Präfix erreichbar), `decodeURIComponent` wirft bei kaputtem Escape und beendet den Test-Server · Paket 4 · low → Scope · in Paket 6 aufgenommen (Drain) · behoben in Paket 6 (999823e)

## Pakete

### [x] 1. Build, Release und CI absichern
- Findings: BUILD-002 (medium), BUILD-003 (low), DX-002 (low), DEP-001 (low)
- Ziel: Lint-Baseline grün, Publish-Reihenfolge und -Fehlerbehandlung verlässlich, CI auf Pull Requests, Dependencies passend zum Geschippten.
- Bereich: `biome.json`, `nx.json`, `.github/`, `scripts/`, `packages/*/package.json`, `packages/*/.npmignore`, `TODO.md`; mitgezogen: `e2e/tests/npm-packages.spec.js`, `packages/rainbow-line/scripts/buildPackage.mjs`, Changelogs, `README.md`, `packages/astro-rainbow-line/README.md`, `CLAUDE.md`, `.gitignore`, `pnpm-lock.yaml`
- Detail: docs/remediation/paket-1.md
- Nebenbefund aufgenommen (gleiche Ursache wie BUILD-003): `scripts/publishNpmPkg.mjs:49-76` `preparePackageRoot()` kopiert beim Publish nach, was `buildPackage.mjs` schon liefert — `.npm-pkg/` weicht dadurch vom Veröffentlichten ab
- Hängt ab von: —
- Begründung für unter fünf Findings: echte Abhängigkeit — die grüne Lint-Baseline trägt die Verify-Gates aller folgenden Pakete, und die Publish-Reihenfolge muss stehen, bevor Paket 4 Versionen bumpt.
- Hash: cdafb38
- Ergebnis: 1 Runde · BUILD-002, BUILD-003, DX-002, DEP-001 und der Nebenbefund `preparePackageRoot()` behoben (Reviewer: freigeben, keine kritischen/wichtigen Befunde) · rote Läufe vor der Änderung: Taskgraph-Prüfung `rainbow-line:publishNpmPkg` ohne `offscreen-display:publishNpmPkg` (Exit 1) und `e2e/tests/npm-packages.spec.js` (4 Tests rot: `repository`, `CHANGELOG.md` in `rainbow-line/.npm-pkg`, Peer-Dependencies) · `pnpm lint` jetzt grün (`audit.html` ausgenommen) · Abweichung: `peerDependenciesMeta` mehrzeilig, weil Biome die einzeilige Form ablehnt
- Nebenbefunde: keine
- Folgen: keine
- Schnittstellen: `packages/rainbow-line/package.json` — `@spearwolf/eventize` und `@spearwolf/offscreen-display` stehen in `devDependencies` und als optionale `peerDependencies` (Peer offscreen-display `workspace:*`, beim Paketbau zu `^<version>` aufgelöst), kein `dependencies`-Block mehr · e2e `npm-packages.spec.js` prüft `peerDependencies`, `repository` und `CHANGELOG.md` im `.npm-pkg/` · nx-Target `publishNpmPkg` hängt von `^publishNpmPkg` ab · `.npm-pkg/` ist das vollständige Veröffentlichungsverzeichnis (`publishNpmPkg.mjs` kopiert nichts nach, rainbow-lines `buildPackage.mjs` kopiert `CHANGELOG.md`) · `makePackageJson.mjs` ohne Präfix-Strip · `packages/astro-rainbow-line/CHANGELOG.md` existiert (Keep-a-Changelog, `## [Unreleased]`) · alle drei Manifeste haben `repository` · `TODO.md` gitignoriert

### [x] 2. offscreen-display: Lebenszyklus, Frame-Loop und Größenmessung
- Findings: RES-001 (high), RES-002 (low), CORR-002 (medium), PERF-002 (medium), CORR-005 (low), TEST-001 (medium, Teil: Worker-Lifecycle, Listener-Fehler, `expect.poll` statt `sleep`, Firefox und WebKit)
- Ziel: Ein entferntes Element beendet seinen Worker, der Frame-Loop übersteht Listener-Fehler, und die Canvas-Größe kommt per `ResizeObserver` in physischen Pixeln — abgesichert durch Lifecycle-Tests und e2e in Chromium, Firefox und (in CI) WebKit.
- Bereich: `packages/offscreen-display/src/`, `packages/*/test/`, `e2e/`; Folgeanpassung der Zeichenroutine in `packages/rainbow-line/` an `pixelRatio`; mitgezogen: `nx.json`, `package.json`, `.github/workflows/main.yml`, `CLAUDE.md`, `README.md`, Changelogs
- Detail: docs/remediation/paket-2.md
- Hängt ab von: —
- Hash: 7b2b0ce
- Ergebnis: 1 Runde · RES-001, RES-002, CORR-002, PERF-002, CORR-005 und der Paket-2-Teil von TEST-001 behoben (Reviewer: freigeben, keine kritischen/wichtigen Befunde, 7 kleine in der Paketdatei) · Regressionstests vor dem Fix rot: in `offscreen-display.test.js` `terminates its worker one animation frame after it was removed`, `starts a fresh worker with a fresh canvas when it is connected again`, `dispose() terminates the worker of a connected element`, `keeps rendering frames after an onFrame listener threw`, `destroy() ends the frame loop and releases the signals of the display`, `measures the canvas in css pixels times devicePixelRatio where the device pixel box is not supported` und der angepasste Resize-Test; in `rainbowLineBehaviour.js` `terminates its worker once it has been removed` (beide Builds); e2e `terminates the workers of removed elements` und `draws its color slices in physical pixels` (Chromium und Firefox) · Abweichung: HiDPI-e2e steht in eigener Datei `e2e/tests/rainbow-line-hidpi.spec.js`, weil Playwright unter reiner `deviceScaleFactor`-Emulation `devicePixelContentBoxSize` in CSS-Pixeln meldet und echte Skalierung `launchOptions` braucht, die in einem `describe` nicht erlaubt sind · WebKit lokal nicht gelaufen (Entscheidung), CI sieht es zuerst
- Nebenbefunde: → Queue
- Folgen:
  - `packages/offscreen-display/CHANGELOG.md:16` und `:30` — zwei `### Changed` unter `[Unreleased]` (Paket 1 und Paket 2), vor dem Datieren in Paket 4 zu einem zusammenlegen → Paket 5
  - `packages/rainbow-line/CHANGELOG.md:14` — Leerzeile mitten in der Fixed-Liste, macht sie zur »loose list« → Paket 5
  - `packages/rainbow-line/CHANGELOG.md:19` — Eintrag aus Paket 1 »… instead of `es2017` …« blickt auf den Vorzustand zurück (Konvention, veröffentlichte Doku); Folge von Paket 1 → Paket 5
  - `README.md:22` — »The tests run in headless chromium, firefox and webkit« stimmt nur für e2e, die Vitest-Paket-Tests laufen in Chromium → Paket 5 (die Zeile steht inzwischen bei `:19`)
  - `e2e/playwright.config.js:10` — das `console.warn` zum ausgelassenen WebKit erscheint je Playwright-Worker-Prozess (13- bis 29-mal pro Lauf), soll einmal erscheinen → Paket 5 (die Zeile steht inzwischen bei `:8`)
  - `packages/offscreen-display/test/offscreen-display.test.js` — der ersetzte Test für das Anhalten der Frames bei `{isConnected: false}` hat keinen Nachfolger; die Worker-Seite dieser Nachricht ist ungetestet (Vorschlag des Reviewers: `display.worker.postMessage({isConnected: false})` senden, stehende Frames prüfen) → Paket 5
  - `packages/astro-rainbow-line/rainbow-line-v0.4.0.js` trägt den alten Code — erneuert Paket 4 mit dem Bump (steht dort schon) → Paket 4
- Schnittstellen: `OffscreenDisplay#dispose()` neu (idempotent; das Element ruft es einen rAF nach dem Trennen selbst, ein späteres Verbinden startet frischen Worker mit frischer Canvas, `this.canvas` wechselt dann) · Main-Thread hat keinen rAF-Loop mehr, die Größe kommt von einem `ResizeObserver` (`device-pixel-content-box`, Fallback Content-Box × `devicePixelRatio`) · Nachricht `{resize: {width, height, pixelRatio}}` in physischen Pixeln · `OffscreenWorkerDisplay#destroy()` und Property/Signal `pixelRatio` neu; `canvasWidth`/`canvasHeight` sind physische Pixel; `onFrame` erst nach der ersten `{resize}`; `onResize` feuert auch bei geändertem `pixelRatio`; ein werfender `onFrame`-Listener beendet den Loop nicht, der Fehler kommt als `error`-Event am `Worker` an · `RainbowLineWorkerDisplay#onFrame` destrukturiert `pixelRatio`, `color-slice-width >= 1` wird zu `Math.max(1, Math.round(colorSliceWidth * pixelRatio))` · Test-Fixture `test-display.worker.js` versteht `{throwInNextFrame: true}` und `{destroy: true}` · `TestDisplay` in `offscreen-display.test.js` sammelt `errors` und `terminatedWorkers` · Playwright-Projekte `chromium`, `firefox`, `webkit`; `E2E_SKIP_WEBKIT=1` lässt webkit lokal aus (nx-Input des e2e-Targets) · `pnpm playwright:install` installiert alle drei, CI mit `--with-deps`

### [x] 3. rainbow-line: Attribute validieren, Zeichenroutine umbauen, Astro-Skript einmal laden
- Findings: CORR-001 (high), CORR-003 (medium), CORR-004 (medium), PERF-001 (medium), READ-001 (low), CONS-001 (low), TEST-001 (medium, Teil: Negativ-Tests für die Wertebereiche aus CORR-001 und CORR-003, Test für einen Laufzeitwechsel von `cycle-colors-repeat`)
- Ziel: Kein Attributwert bringt den Worker zum Hängen oder Abstürzen, jede Frame zeichnet mit zwei `drawImage` aus einem vorgerenderten Streifen, und `<RainbowLine>` gibt seinen Skript-Tag einmal pro Seite aus.
- Bereich: `packages/rainbow-line/src/` (neu: `src/attributes.js`), `packages/rainbow-line/test/`, `packages/rainbow-line/scripts/buildPackage.mjs`, `packages/astro-rainbow-line/`, `e2e/tests/astro-rainbow-line.spec.js`, `e2e/tests/npm-packages.spec.js`, READMEs und Changelogs beider Packages
- Detail: docs/remediation/paket-3.md
- Hängt ab von: Paket 2 (Zeichenroutine rechnet mit `pixelRatio`)
- Hinweis aus Zug 0 von Paket 2: der TEST-001-Teil kam aus Paket 2 hierher, weil diese Tests die Regressionstests der CORR-001/003/004-Fixes sind und vor ihnen rot sein müssen · Paket 2 rechnet `color-slice-width` ab 1 mit `Math.max(1, Math.round(colorSliceWidth * pixelRatio))` in physische Pixel um, der Umbau der Zeichenroutine behält das bei
- Hinweis aus Zug 0 von Paket 3: die Farbscheiben bleiben an ihrem Platz und wechseln nur ihre Farbe, wie es die Tests mit exakten `colorRuns` festschreiben; die zwei `drawImage` laufen deshalb über eine Zwischen-Canvas mit einem Pixel je Scheibe statt über den wandernden Streifen der Audit-Empfehlung · die Attribut-Tabelle im rainbow-line-README zieht dieses Paket aus DX-001 (Paket 4) vor, weil es die Wertebereiche festlegt
- Hash: c667c20
- Ergebnis: 1 Runde · CORR-001, CORR-003, CORR-004, PERF-001, READ-001, CONS-001 und der Paket-3-Teil von TEST-001 behoben (Reviewer: freigeben, keine kritischen/wichtigen Befunde, 4 kleine in der Paketdatei) · Regressionstests vor dem Fix rot: in `rainbowLineBehaviour.js` (beide Builds, 26 rot) `falls back to 10px slices for a color-slice-width of 0`/`-10`, `draws slices of at least one pixel for a color-slice-width that rounds to zero`, `falls back to the default cycle time for a slice-cycle-time of 0`, `falls back to one repetition for a cycle-colors-repeat of 0`, `accepts cycle-colors separated by commas, tabs and line breaks` (4 Fälle), `leaves out invalid colors of cycle-colors`, `shows the rainbow when cycle-colors has no valid color`, `applies a cycle-colors-repeat that changes after the element has been connected`, `sets a numeric attribute back to its default when it is removed or out of range`; `RainbowLine.test.js` `renders the script tag only once per page`; e2e `loads the rainbow-line script with a single script tag` (Chromium, Firefox) und `the published source modules of rainbow-line only import published files` · Wächter vor dem Fix grün: `slice-cycle-time of -3`, `cycle-colors-repeat of -2`, `keeps css color functions in cycle-colors together`, `keeps the color slices in place while their colors cycle` · Abweichungen: `Astro.locals[...] ??= new Set()` als eigene Anweisung (Biome `noAssignInExpressions`), `clearRect` vor dem Palettenrendern, Attributnamen in einer Modulkonstante `OBSERVED_ATTRIBUTES`
- Nebenbefunde: → Queue
- Folgen:
  - `packages/astro-rainbow-line/RainbowLine.astro:31-32` — `Astro.locals[RENDERED_SCRIPTS]` mit Symbol-Index steht ohne `// @ts-ignore`; unter `astro/tsconfigs/strict` in einem Konsumentenprojekt möglicherweise TS7053 (unbelegt, `pnpm typecheck` sieht `.astro` nicht) — vor dem Release prüfen, bei Fehler `// @ts-ignore` wie die übrigen Stellen der Datei → Paket 4 (Typen und Release von astro-rainbow-line)
- Schnittstellen: neues Modul `packages/rainbow-line/src/attributes.js` (veröffentlicht, in `COPY_FILES`) mit `DEFAULT_COLOR_SLICE_WIDTH = 10`, `DEFAULT_SLICE_CYCLE_TIME = 7`, `DEFAULT_CYCLE_COLORS_REPEAT = 1`, `toPositiveNumber(value, defaultValue)` · `RainbowLineElement.observedAttributes` hat fünf Namen samt `cycle-colors-repeat`; ungültige oder entfernte Zahlenattribute gehen als Default an den Worker · Worker: `parseMessageData(data)` unverändert exportiert, Zeichenroutine über Palette (1024 px) → Streifen → Zwischen-Canvas (ein Pixel je Scheibe) → zwei `drawImage`; `cycle-colors` per Klammer-bewusstem Tokenizer, ungültige Farben mit `console.warn` übersprungen · `RainbowLine.astro` merkt ausgegebene Skripte in `Astro.locals[Symbol.for('@spearwolf/astro-rainbow-line:scripts')]` (ein `Set` der `src`-Pfade), `BASE_URL`-Fallback `'/'` · e2e `npm-packages.spec.js` prüft, dass jede relative `from '…'`-Quelle der veröffentlichten `./src/`-Exports im `.npm-pkg/` liegt · rainbow-line-README hat den Abschnitt `## Attributes` (Tabelle mit Defaults und Wertebereichen) · vendored `rainbow-line-v0.4.0.js` trägt weiter den alten Code (Paket 4)

### [x] 5. Nachtrag zu Paket 2: Changelogs, Testaussage im README, WebKit-Hinweis, Test für angehaltene Frames
- Findings: — (Folgen aus Paket 2, dazu zwei vorbestehende Nebenbefunde mit derselben Ursache)
- Folge von: Paket 2
- Detail: docs/remediation/paket-5.md
- Ziel: Was Paket 2 halb fertig hinterlassen hat, ist zu Ende gebracht — die Changelogs folgen Keep-a-Changelog und den Konventionen, das README sagt richtig, wo welche Tests laufen, der Hinweis auf das ausgelassene WebKit erscheint einmal pro e2e-Lauf, und die Worker-Seite von `{isConnected: false}` ist getestet.
- Fundstellen:
  - `packages/offscreen-display/CHANGELOG.md:16` und `:30` — zwei `### Changed` unter `[Unreleased]`, zu einem zusammenlegen
  - `packages/rainbow-line/CHANGELOG.md:14` — Leerzeile mitten in der Fixed-Liste (»loose list«)
  - `packages/rainbow-line/CHANGELOG.md:23` (vorher `:19`) — »… instead of `es2017` …« blickt auf den Vorzustand zurück; laut Blame vorbestehend (`a785bdf`), nicht aus Paket 1 → Nebenbefund, im Paket behalten, weil Paket 4 genau diesen Abschnitt datiert
  - `README.md:19` — »The tests run in headless chromium, firefox and webkit« stimmt nur für e2e, die Vitest-Paket-Tests laufen in Chromium
  - Nebenbefund aufgenommen (gleiche Ursache wie `README.md:19`): `README.md:37` — »vitest browser mode« für jedes Package, `astro-rainbow-line` läuft in Node; vorbestehend (`a785bdf`)
  - `e2e/playwright.config.js:8` — das `console.warn` zum ausgelassenen WebKit erscheint je Playwright-Worker-Prozess (13- bis 29-mal pro Lauf), soll einmal erscheinen
  - `packages/offscreen-display/test/offscreen-display.test.js` — kein Test mehr für das Anhalten der Frames nach `{isConnected: false}` (Vorschlag des Reviewers aus Paket 2: `display.worker.postMessage({isConnected: false})` senden, stehende Frames prüfen)
- Bereich: die Dateien der Fundstellen, dazu die Test-Fixture `packages/offscreen-display/test/fixtures/test-display.worker.js`
- Hängt ab von: Paket 2; nach Paket 3, weil Paket 3 dieselbe Fixed-Liste in `packages/rainbow-line/CHANGELOG.md` erweitert
- Restplan nach Zug 0 von Paket 5: Reihenfolge 5 → 4 bleibt; die umgeordnete `es2017`-Stelle und `README.md:37` ändern keinen Schnitt, sie liegen in Dateien, die dieses Paket ohnehin anfasst
- Begründung für das eigene Paket: fünf der sieben Stellen sind Symptome von Paket 2 (hätte es seine Änderung zu Ende gebracht, gäbe es keine davon), Paket 2 ist committet, also ein Nachtragspaket, das alle Fundstellen aufzählt — vor Paket 4, das die aufgeräumten Changelogs datiert
- Hash: 8c34e81
- Ergebnis: 1 Runde · alle sieben Fundstellen behoben (Reviewer: erfüllt, keine kritischen/wichtigen Code-Befunde, keine kleinen) · Test `the worker stops its frames on {isConnected: false} and resumes them on {isConnected: true}` per Mutationsprobe rot (`'isConnected' in data` → `data.isConnected`: 25 statt 7 Frames) · WebKit-Hinweis 29 → 1 pro Lauf
- Nebenbefunde: keine
- Folgen: keine
- Schnittstellen: Fixture `test-display.worker.js` quittiert jede `{isConnected}`-Nachricht mit ihrem Wert · `e2e/playwright.config.js` meldet das ausgelassene WebKit nur im Hauptprozess (`!process.env.TEST_WORKER_INDEX`)

### [x] 4. Typen, Dokumentation und Release-Stand
- Findings: TYPES-001 (medium), DX-001 (medium), BUILD-001 (medium)
- Ziel: Die Packages liefern Typdeklarationen, die READMEs beschreiben die tatsächliche API, und Versionen, Changelogs sowie das vendored Bundle stehen auf dem Release-Stand.
- Detail: docs/remediation/paket-4.md
- Bereich: `tsconfig.json`, `nx.json`, `packages/*/tsconfig.build.json` (neu), `packages/*/scripts/buildPackage.mjs`, `packages/*/package.json`, `packages/rainbow-line/project.json`, JSDoc in den Quellen von offscreen-display und rainbow-line, `packages/astro-rainbow-line/` (Skriptpfad, vendored Bundle, `Astro.locals`), READMEs und Changelogs aller drei Packages, `e2e/tests/npm-packages.spec.js`, `e2e/types/consumer.ts` (neu), `CLAUDE.md`; für `noImplicitAny` Annotationen in `e2e/`, `scripts/`, `testing/`
- Aufgenommen in Zug 0: Folge aus Paket 3 `packages/astro-rainbow-line/RainbowLine.astro:31-32` (TS7053 unter `strict`, belegt; im Gegenstand von TYPES-001 und in einer Datei, die das Paket ohnehin ändert) · Nebenbefunde `OffscreenDisplay.js:81`/`:167`, `packages/rainbow-line/README.md:38`, Vergleichslinks in `packages/rainbow-line/CHANGELOG.md`, `packages/astro-rainbow-line/README.md:5`
- Hängt ab von: Paket 2, 3 und 5 (Typen und Doku beschreiben deren API, das vendored Bundle braucht deren Code, die Changelogs datiert es erst, nachdem Paket 5 sie aufgeräumt hat)
- Hinweis aus Zug 0 von Paket 1: die `CHANGELOG.md` für astro-rainbow-line aus DX-001 legt Paket 1 an (samt dem 1.3.0-Eintrag aus dem README), weil die Konvention dort seinen Changelog-Eintrag verlangt; für DX-001 bleibt beim astro-Paket nur der README-Text
- Hinweis aus Zug 0 von Paket 2: die README von offscreen-display muss dann `dispose()`, `destroy()`, Größen in physischen Pixeln samt `pixelRatio` und die Pflicht beschreiben, dass eine eigene Canvas ihre Größe aus CSS bekommt; dass Subklassen jetzt physische Pixel sehen, gehört als Breaking Change in den Minor-Bump von offscreen-display
- Hinweis aus Zug 0 von Paket 3: die Attribut-Tabelle im rainbow-line-README (Defaults, Wertebereiche, Farbsyntax von `cycle-colors`, Kehrwert von `cycle-colors-repeat`) legt Paket 3 an; für DX-001 bleibt beim rainbow-line-README Prüfen und der Rest
- Hinweis aus Zug 0 von Paket 5: der Queue-Eintrag zu den Vergleichslinks in `packages/rainbow-line/CHANGELOG.md` berührt den Abschnitt, den dieses Paket datiert; der Tag `rainbow-line-v0.4.0` existiert im Repo nicht (`git tag -l` endet bei `rainbow-line-v0.3.0`), ein Link auf 0.4.0 oder 0.5.0 zeigt also erst nach dem Tagging durch den Nutzer irgendwohin · der `bundle.js`-Eintrag in rainbow-lines `[Unreleased]` nennt nach Paket 5 die Targets statt »10% smaller«
- Restplan nach Zug 0 von Paket 4: kein weiteres offenes Paket, Schnitt bleibt — kein Teilen trotz der fünf Aufnahmen, weil der Release-Teil ohnehin nach allen Code-Änderungen des Pakets stehen muss · für den Abschluss: ändert der Drain danach noch Code eines Packages, gehören die Changelog-Einträge in die datierten Abschnitte (0.3.0, 0.5.0, 1.4.0), solange der Nutzer nicht gepusht hat, und bei rainbow-line-Code wird das vendored Bundle neu kopiert (siehe Queue) · die Tags `rainbow-line-v0.4.0` (Commit `896de45`, der 0.4.0 gesetzt hat) und `rainbow-line-v0.5.0` (Commit dieses Pakets) gibt es nicht; auf sie zeigen die Vergleichslinks, setzen muss sie der Nutzer
- Begründung für unter fünf Findings: echte Abhängigkeit — Release-Stand und Doku können erst entstehen, wenn aller Code steht.
- Hash: 07f8a56
- Ergebnis: 1 Runde · TYPES-001, DX-001, BUILD-001, die Folge aus Paket 3 (`RainbowLine.astro` unter `strict`) und vier Nebenbefunde behoben (Reviewer: freigeben, keine kritischen/wichtigen Befunde, 5 kleine in der Paketdatei) · Regressionstests vor dem Fix rot: e2e `the published type declarations serve a typescript consumer` (10 Fehler: TS7016 an den benannten Imports, TS4112, TS2339, TS2578) und `astro-rainbow-line vendors the rainbow-line bundle of the current rainbow-line version` (nach dem Bump, vor dem Vendoring) · `RainbowLine.astro` per Scratch-Probe unter `--strict` alt rot (TS7053), neu grün · `pnpm typecheck` mit `noImplicitAny` 0 Fehler · Tags `rainbow-line-v0.4.0` (auf `896de45`) und `rainbow-line-v0.5.0` (auf `07f8a56`) muss der Nutzer setzen, sonst zeigen die Vergleichslinks im rainbow-line-Changelog ins Leere · Veröffentlicht wird erst mit dem Push des Nutzers
- Nebenbefunde: → Queue
- Folgen: keine
- Schnittstellen: `packages/offscreen-display/tsconfig.build.json` und `packages/rainbow-line/tsconfig.build.json` (erben von `tsconfig.json`, `build` ruft nach esbuild `tsc -p tsconfig.build.json`, Ausgabe `dist/types/`; rainbow-line mit `paths: {}` nur für `attributes.js`, `RainbowLineElement.js`, `RainbowLineWorkerDisplay.js`) · `exports` mit `types` als erster Bedingung (offscreen-display `.` und `./worker.js` plus Top-Level-`types`; rainbow-line nur die zwei Quell-Subpfade) · `buildPackage.mjs` beider Packages kopiert `dist/types` nach `.npm-pkg/dist/types` und scheitert ohne sie · `tsconfig.json` `noImplicitAny: true`, `strict: false` · `rainbow-line/project.json` `build.outputs` mit `{projectRoot}/dist` · `nx.json` `sharedGlobals` mit `tsconfig.json` · exportierte Typen `OffscreenDisplayMessage` und `OffscreenWorkerDisplayEvents` aus `@spearwolf/offscreen-display/worker.js` · `OffscreenWorkerDisplay#ready` ist `boolean`, statische Event-Namen als Literaltypen, Signal-Properties als typisierte Klassenfelder · `OffscreenDisplay#canvas` im Konstruktor deklariert · e2e-Fixture `e2e/types/consumer.ts` · Versionen offscreen-display 0.3.0, rainbow-line 0.5.0, astro-rainbow-line 1.4.0; Changelogs datiert 2026-09-22 mit leerem `[Unreleased]` darüber · vendored `packages/astro-rainbow-line/rainbow-line-v0.5.0.js` (Kopie von `packages/rainbow-line/bundle.js`), `RainbowLine.astro` Default `js/rainbow-line-v0.5.0.js`; `npm-packages.spec.js` erzwingt den Gleichstand mit der rainbow-line-Version

### [x] 6. Nebenbefunde aus der Queue: Worker-Pfad des Quell-Subpfads, Test-Server, Doku-Reste
- Nebenbefund: die sieben offenen Einträge unter »Offene Befunde« (Worker-URL in `RainbowLineElement.createWorker()` für den veröffentlichten Quell-Subpfad, `e2e/server.mjs` Pfadprüfung und `decodeURIComponent`, `parseMessageData` bei `null`/`undefined` in `RainbowLineWorkerDisplay.js`, Tippfehler `adpoptedCallback`, `CLAUDE.md:68` Banner, `CLAUDE.md:32` Typecheck der Tests, `offscreen-display/README.md:5` »offline canvas«)
- Ziel: Die in diesem Lauf aufgefallenen Nebenbefunde sind behoben; rainbow-line-Code-Änderungen landen im datierten 0.5.0-Changelog-Abschnitt, und das vendored `rainbow-line-v0.5.0.js` wird aus dem neuen `bundle.js` kopiert.
- Bereich: `packages/rainbow-line/src/`, `packages/rainbow-line/scripts/`, `packages/rainbow-line/test/`, `packages/rainbow-line/README.md`, `packages/astro-rainbow-line/rainbow-line-v0.5.0.js`, `packages/offscreen-display/` (Worker-`parseMessageData`, Kommentar, README, Test), `e2e/server.mjs`, `e2e/resolveFile.mjs` (neu), `e2e/tests/server.spec.js` (neu), `e2e/tests/npm-packages.spec.js`, `e2e/playwright.config.js`, `CLAUDE.md`, Changelogs von rainbow-line (0.5.0) und offscreen-display (0.3.0)
- Detail: docs/remediation/paket-6.md
- Nebenbefund aufgenommen in Zug 0 (gleiche Ursache wie `RainbowLineWorkerDisplay.js:97`): `packages/offscreen-display/src/lib/worker/OffscreenWorkerDisplay.js:196` — `parseMessageData` fängt nur Falsy-Werte ab, `'isConnected' in data` wirft bei `42`, `'text'`, `true`; vorbestehend (`e57a13c`)
- Hängt ab von: Paket 4 (Release-Stand, vendored Bundle)
- Restplan nach Zug 0 von Paket 6: kein weiteres offenes Paket, Schnitt bleibt — acht kleine Stellen in einem Implementierer-Lauf sind billiger als zwei Pakete mit je drei Kaltstarts · alle Einträge unverändert vorhanden, keiner gegenstandslos · `main` ist 5 Commits vor `origin/main`, die Changelog-Einträge gehören deshalb in die datierten Abschnitte 0.3.0 und 0.5.0
- Hash: 999823e
- Ergebnis: 1 Runde (der erste Implementierer-Prozess endete ohne Report und wurde per `--resume` derselben Session zum Report gebracht, keine Nachbesserung) · alle sieben Queue-Einträge und der in Zug 0 aufgenommene Nebenbefund `OffscreenWorkerDisplay.js:196` behoben (Reviewer: freigeben, keine Qualitätsbefunde) · Regressionstests vor dem Fix rot: e2e `the published source modules of rainbow-line only reference published files` (`rainbow-line.worker.js referenced by ./src/RainbowLineElement.js exists`), vitest `parseMessageData() ignores message data that is not an object` (offscreen-display, TypeError bei `42`), vitest `ignores worker messages that are not objects` (rainbow-line, beide Builds, vier TypeErrors), e2e `resolveFile() stays inside the directory of a route, also next to a neighbour with the same name prefix` und `the server answers a malformed escape with 400 and keeps running` (`socket hang up`) · vendored `rainbow-line-v0.5.0.js` neu aus `bundle.js` kopiert · Changelog-Einträge in den datierten Abschnitten offscreen-display 0.3.0 und rainbow-line 0.5.0 · Veröffentlicht wird erst mit dem Push des Nutzers
- Nebenbefunde: keine
- Folgen: keine
- Schnittstellen: `.npm-pkg/src/rainbow-line.worker.js` wird mitveröffentlicht (`COPY_FILES` in `packages/rainbow-line/scripts/buildPackage.mjs`), `RainbowLineElement#createWorker()` lädt `new URL('./rainbow-line.worker.js', import.meta.url)` · `OffscreenWorkerDisplay#parseMessageData()` und rainbow-lines `parseMessageData()` ignorieren Daten, die kein Objekt sind (rainbow-line-JSDoc jetzt `Record<string, any> | null | undefined`) · neues Modul `e2e/resolveFile.mjs` mit `resolveFile(routes, urlPath)`, `e2e/server.mjs` antwortet auf kaputte Escapes mit 400 · neue Spec `e2e/tests/server.spec.js`, wie `npm-packages.spec.js` nur im chromium-Projekt (`testIgnore` in `e2e/playwright.config.js`)
